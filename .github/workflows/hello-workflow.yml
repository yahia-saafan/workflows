name: Hello Workflow

on: 
  workflow_dispatch:

jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello World
        run: echo "Hello, World!"
      - name: Print GoodBye
        run: |
            echo "First Bye!"
            echo "Second Bye!"
